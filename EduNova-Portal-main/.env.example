# EduNova Environment Variables
# The Gemini key must remain on the backend. Do not add it as REACT_APP_*:
# Create React App bundles REACT_APP_* values into public browser JavaScript.
REACT_APP_API_URL=http://localhost:5000/api
